apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jaco
  namespace: jaco
  annotations:
    # Cloudflare terminates TLS — Traefik only needs HTTP (port 80)
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  rules:
    # Replace <YOUR_DOMAIN> with your actual domain
    - host: <YOUR_DOMAIN>
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: jaco
                port:
                  number: 80
